#Function that just says hello exists:
#def hello(to="world"):    # inside () is new parameter    
    #everything under and tabbed is part of the new function
    # you can guve parameter default meaning.
    #print("Hello,", to)     # uses the new parameter.
    
    

def main():   #this only declares doesn't call
    hello()
    name = input("What's your name?  ")
    hello(name)

    #Function that just says hello exists:
def hello(to="world"):    # inside () is new parameter    
    #everything under and tabbed is part of the new function
    # you can guve parameter default meaning.
    print("Hello,", to)     # uses the new parameter.

    #scope means a variable only exists in the scope which is declared.

main() #this calls main and the hello() function is already declared.
    


#if you use a function you must decalre them by the time they're called.
# def = define






